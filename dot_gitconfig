[user]
    name = doxxx93
    email = doxxx93@gmail.com

[commit]
    gpgsign = true

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[credential "https://github.com"]
    helper =
    helper = !gh auth git-credential

[credential "https://gist.github.com"]
    helper =
    helper = !gh auth git-credential

# To use different git config for work directory:
# 1. Create ~/.gitconfig-work with [user] name and email
# 2. Add below to local ~/.gitconfig (not this dotfile)
#
# [includeIf "gitdir:~/work/"]
#     path = ~/.gitconfig-work
